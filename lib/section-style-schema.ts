// 每個 section 的元素級樣式覆寫：欄位表與清洗規則的單一來源。
//
// 為什麼收到這裡：同一份欄位表原本在四個地方各抄一份——公開頁 _theme.ts 的 SectionStyle
// 型別、_theme.ts 讀回時的 sanitize、editor actions.ts 存檔時的 sanitize、editor 自己的
// patch 型別。每個欄位的合法值（"compact" | "default" | "spacious" 這種）在讀、寫兩層各
// 寫成一條手打的 or 判斷，加一個控制就要在兩條長鏈各補一段，而且字串要一字不差。漏在讀
// 那層，商家設好的值存得進 DB 卻讀不回來（畫面完全沒反應）；漏在寫那層，畫面上點得動、
// 自動存檔也顯示已存，重新整理就沒了——兩種都不會報錯，只會「這個控制好像壞的」。
// 兩份手抄清單靠人工對齊，本來就是遲早會漏的結構（專案裡 format-price、store-schema、
// fetch-order-items 都是同一個出發點收掉的）。
//
// 收成這一份之後：欄位與合法值只寫在 SECTION_STYLE_ENUMS，型別由它推出來，讀寫兩層都呼叫
// 同一支 sanitizeSectionStyles。加一個控制＝在這裡加一行，剩下的是編輯器要不要給 UI。
import { normalizeHexColor } from "./hex-color";

// 每個欄位的合法值。第一個值不一定是預設——「沒設定」在這套系統裡是「這個 key 不存在」，
// 不是某個特定值（editor 端把等同預設的選擇 delete 掉，公開頁沒讀到就整條樣式不套）。
export const SECTION_STYLE_ENUMS = {
  headingAlign: ["left", "center", "right"],
  // 內文對齊（auto 跟著上面那條區段對齊走 / left 靠左 / center 置中 / right 靠右）。
  // headingAlign 設的是整段容器的 text-align，段落是繼承來的，所以標題與內文一直只能同進退；
  // 報紙與雜誌最常見的「標題置中、內文靠左」在 Sproutly 做不出來。這一欄只管內文元素。
  bodyAlign: ["auto", "left", "center", "right"],
  // 內文一行字數（auto 不限制 / normal 約 34 字 / narrow 約 24 字）。滿版區段的長段落一行
  // 會拉到整個螢幕寬，眼睛換行時找不到下一行的行首，讀起來一直在跳行；報紙與雜誌都是把
  // 內文收成窄欄解這件事。限制的是段落自己的寬度，不是整段區段（sectionWidth 收的是整段，
  // 連標題、卡片、照片一起變窄，做不出「標題滿版、內文窄欄」）。
  bodyMeasure: ["auto", "normal", "narrow"],
  // 內文字級（default 不套 / small 縮小一成 / large 放大一成多）。標題已經有 headingScale
  // 可以各段獨立調大小，內文一直只能跟著全網站走：長描述在手機上偏小、想讓某一段的短文
  // 當引言放大也做不到，商家唯一的辦法是把整段的字體設定改掉、連標題一起變。
  // 這一欄只縮放內文自己，而且是等比縮放——同一段裡描述、圖說、引言原本的大小差距照原樣
  // 保留，不會被壓成同一級（見 layout.tsx 那條規則裡為什麼不能用 font-size 的說明）。
  bodyScale: ["small", "default", "large"],
  // 內文濃淡（default 不套 / muted 更淡 / strong 跟標題同深）。描述、說明、圖說這類次要文字
  // 一律用比標題淡的那個顏色（--store-text-muted，約七成濃度）——那是排版上的層級設計，
  // 但同一個濃度不是每間店都讀得動：淺灰底配淺灰字、或客人年紀偏大時，商品描述整段是糊的。
  // 商家原本能動的只有兩個極端：「文字顏色」把整段（含標題）一起換掉，或「淡化」把整段
  // 連照片一起變透明——沒有一個只調次要文字的。這一欄補的就是那一格。
  bodyTone: ["muted", "default", "strong"],
  // 內文粗細（normal 照原本的 400 常規 / medium 500 中黑 / bold 700 粗），只套段落自己的
  // 內文（描述、引言、圖說、常見問題的答案），不動標題也不動卡片裡那幾行。
  // 粗細這一格前面補過四個位置：段落大標（headingWeight）、卡片品名（cardTitleWeight）、
  // 卡片描述（cardDescWeight）、卡片價錢（cardPriceWeight）——段落自己的內文是這一組裡
  // 最後一個沒得動的，偏偏是每一段字數最多、客人真的會讀完的那幾行。
  // 缺這格會怎樣：那幾行一律 400，而且多半同時被淡成 --store-text-muted（文字色的七成），
  // 兩個減法疊起來，段落引言與常見問題的答案在淺色底上是一片糊的灰。商家原本能救的只有
  // 三個方向，每一個都連帶動到別的東西：「內文濃淡」調的是顏色深淺（調濃會跟標題撞成
  // 同一層，本來的主次沒了）、「內文大小」把字放大（整段跟著變高，一列卡片的版面被撐開）、
  // 「字體」換成黑體（整段連標題一起換）。粗細不佔空間、不換顏色，是讓一段字站出來最省的
  // 作法，卻是唯一沒有的一格。反過來也一樣：把整段當襯底、想讓內文再退一步的段落
  //（合作 logo、數字那種），現在也只能靠淡化整段連照片一起變透明。
  // 規則落在跟「行高」「內文對齊」同一份選擇器上（p / li / blockquote / figcaption / dd），
  // 三格管的是同一批字，一起調才講得通。卡片那三格的規則帶 class（.sproutly-card-desc /
  // .sproutly-card-price），比這條精確，商家兩邊都設時卡片照樣聽卡片那格的。
  // 只用有載進來的字重（400 / 500 / 700），不給 300 那種細的——沒載的字重瀏覽器會拿常規
  // 去假變細，中文筆畫會糊掉，跟大標與卡片那三格同一個理由。
  bodyWeight: ["normal", "medium", "bold"],
  // 標題用色（default 跟整段文字色 / accent 全站主色 / muted 跟次要文字同深淺）。
  // 區段裡用主色畫的一直只有配件——小標 eyebrow、標題底下那截短線、常見問題的＋——標題
  // 本身固定用文字色，商家想讓某一段的標題帶品牌色（最常見的「標題用主色、內文用黑」），
  // 唯一動得到標題顏色的是「文字顏色」，但那欄換的是整段：內文、圖說全部跟著變，等於
  // 做不到。這一欄只動標題；muted 給想讓標題退後一步、把重量讓給照片的段落（相簿、合作）。
  headingTone: ["default", "accent", "muted"],
  // 該 section 獨立上下空白（覆寫全網站值）
  paddingScale: ["compact", "default", "spacious"],
  // 標題與內容的距離（tight 收緊 / normal 照這一段原本的 / loose 放寬），指的是段落最上面
  // 那塊（小標 + 大標 + 引言）跟底下卡片、照片、問答之間空多少。每一段的那個距離是寫死的
  // 一個值，而且各段差很多：選物 128px、精選與慢讀 112px、常見問題 64px、合作 48px——那組
  // 值是照站上預設內容挑的，換成商家自己的東西常常不對。標題只有兩個字、底下四張卡的段落，
  // 中間空一大片會看起來像兩段沒關係的東西；反過來標題底下還有兩三行引言的段落，距離太近時
  // 引言跟卡片黏在一起，客人分不出哪句話在說明什麼。
  // 商家原本沒有一格動得到：「區段空白」跟「上下外距」調的是段落外圍的上下，段落裡面一動
  // 也不動；「卡片間距」調的是卡片彼此之間；「一行字數」「內文對齊」動的是文字自己。
  // 沒設就沒 attribute、整條規則不存在，各段維持自己原本那個值。
  headingGap: ["tight", "normal", "loose"],
  // 標題塊裡面的距離（tight 收緊 / normal 照這一段原本的 / loose 放寬）。上面那欄調的是整塊
  // 標題「對外」跟卡片之間的距離，這欄調的是那塊「裡面」——小標跟大標之間、大標跟底下引言
  // （慢讀那段）或那截短線（客人的話 / 常見問題 / 數字 / 相簿）之間。這兩段距離同樣是寫死的
  // （小標底下 16-20px、大標底下 24px），照站上預設那種一行小標配一行大標挑的。
  // 商家換成自己的字之後常常不對：小標寫「PLANTAE MARKET / 本月選物」那種長字串、大標又是
  // 兩行的段落，三行字黏成一團分不出誰是標題；反過來只有兩三個字的短標題，中間空著反而讓
  // 小標飄在上面像跟這段沒關係。原本沒有一格動得到——「標題與內容」調的是這塊對外那一段、
  // 「標題大小」換的是字級（字大了間距不會跟著長）、「這段的上下空白」調的是段落外圍。
  // 沒設就沒 attribute、整條規則不存在，各段維持自己原本那兩個值。
  headingInnerGap: ["tight", "normal", "loose"],
  // 小標字距（tight 收緊 / normal 照原本的 0.4em / wide 再撐開），只套段落最上面那行小標
  // （eyebrow），不動大標與內文。那行字一律撐開 0.4em，是照英文短詞（NEW ARRIVALS、
  // OUR STORY）挑的——英文全大寫撐開字距是雜誌上的標準做法，字母之間本來就有空隙。
  // 但商家打的是中文：「本月選物」四個字每個之間硬塞進 0.4 個字寬，看起來不是一個詞，
  // 是四個各自站著的單字；小標寫得長一點（「來自南投的手作盆器」）在手機上直接被撐到
  // 換行，一行小標變兩行，跟底下的大標黏成一團分不出層級。反過來只有兩三個字的英文
  // 小標，撐得更開一點才有那個雜誌感，現在也停在同一個值。
  // 商家原本沒有一格動得到——「字距」那欄設的是整段（大標、內文一起變，小標反而因為
  // 自己帶著 0.4em 動都不動）、「標題塊裡面」調的是小標跟大標之間的上下距離、「標題大小」
  // 換的是大標的字級。
  // 收緊給的是 0.12em 不是 0：小標的樣式標誌就是那個比內文寬的字距，收到 0 會變成一行
  // 普通的小字，看起來像漏排版而不是刻意的。
  eyebrowTracking: ["tight", "normal", "wide"],
  // 小標字級（small 縮一成 / default 照原本的 / large 放大三成），只套段落最上面那行小標。
  // 上一欄調的是那行字彼此之間空多少，這欄調的是那行字本身多大——那行一律是 10px（有兩段
  // 是 11px），跟上一欄的 0.4em 一樣是照英文全大寫短詞挑的：大寫字母沒有下伸部、整排等高，
  // 10px 還讀得出 NEW ARRIVALS；中文在 10px 是另一回事，「本月選物」那種筆畫多的字擠在
  // 10px 裡糊成一團灰塊，客人在手機上看到的是一行看不清楚的東西，而那行偏偏是商家用來
  // 標段落名字的。反過來想把小標當成這段的主標（照片牆、合作那種大標只有兩個字、真正在
  // 說明的是小標的段落），10px 也小得撐不起來。
  // 商家原本沒有一格動得到——「標題大小」換的是大標的字級（小標自己帶著 10px 不跟著動）、
  // 「全網站字體大小」動的是內文那一層、「小標字距」調的是字與字之間的距離。
  // 用 zoom 不用 font-size：那行的 10px 與 11px 是各段自己挑的，寫死一個 font-size 會把
  // 兩種拉成同一級（跟內文字級那欄同一個理由），zoom 是等比放大，各段原本的差別照原樣留著，
  // 底下那截 margin 也跟著縮放，不會出現字變大了、跟大標的距離還停在原位的狀況。
  eyebrowScale: ["small", "default", "large"],
  // 小標粗細（light 常規 400 / normal 照這一段原本的 / medium 中黑 500 / bold 粗 700），
  // 只套段落最上面那行小標。粗細這條線前面補過五個位置——段落大標（headingWeight）、段落
  // 內文（bodyWeight）、卡片品名（cardTitleWeight）、卡片描述（cardDescWeight）、卡片價錢
  //（cardPriceWeight）——小標是最後一個沒得動的，而它偏偏是全段字級最小的那行。
  // 缺這格會怎樣：那行的粗細各段自己寫死，而且分成兩種——選物與精選那兩段的小標是 500，
  // 其餘八段沒設、繼承下來就是 400。兩種都有問題。400 那批：10px 的中文再加上 0.4em 的
  // 字距，筆畫細到在淺色底上幾乎看不見，而那行是商家用來標段落名字的（客人往下捲時第一眼
  // 找的就是它）。500 那批反過來：那兩行是用全站主色印的，再加中黑，在大標旁邊搶得比大標
  // 還前面，商家想讓它退回配角完全沒有辦法。
  // 商家原本沒有一格動得到——「標題粗細」落在大標（h2）、「內文粗細」那條規則的選擇器是
  // p / li / blockquote / figcaption / dd，小標本身就是 <p>，看起來會中，但那一格一動整段
  // 的描述、引言、答案全部跟著變粗，做不到只動小標；「小標字級」把字放大（那行跟著佔更多
  // 位置、跟大標的距離也一起長）、「小標字距」撐開的是字與字之間（字本身的重量沒變）。
  // 給四個值不是三個：那兩段的小標本來就是 500，只給「跟預設 / 中黑 / 粗」的話它們沒有
  // 一個按鈕退得回 400，等於這格對最需要它的兩段是壞的。
  // 只用有載進來的字重（400 / 500 / 700），不給 300 那種細的——沒載的字重瀏覽器會拿常規
  // 去假變細，10px 的中文筆畫直接糊成一團，比原本更看不清楚。
  eyebrowWeight: ["light", "normal", "medium", "bold"],
  // 小標行距（tight 收緊 / normal 照原本的 / loose 拉開），只套段落最上面那行小標，指的是
  // 那行小標排到兩行以上時，上下兩行之間隔多遠。
  // 上兩欄補的是那行字的橫向（字距）與大小，一直沒補的是它換行之後的縱向。而這行偏偏很容易
  // 換行——它自己帶著 0.4em 的字距（等於每個字後面多塞將近半個字寬），四個中文字在手機上
  // 佔的寬度接近六個字，商家打「來自南投的手作盆器」那種九個字的小標，或把「小標字級」按到
  // 大（放大三成）之後，一行放不下是常態。
  // 換行之後的樣子現在是兩種都不對。那行是 <p>，沒帶 leading class，行距整條繼承段落那層
  // 的內文行高（站上預設 1.7 上下、商家把「行高」按到舒展就是 2）——那個值是給一整段要讀的
  // 文字挑的，套在 10px 的標籤上，兩行之間空出的距離比字本身還高，看起來不是一行換到第二
  // 行，是上下兩個沒關係的小標；反過來按到緊湊（1.4）雖然收得回來，代價是整段的描述、
  // 引言、常見問題答案全部一起被收緊，商家要的只是那一行別散開。
  // 商家原本沒有一格單獨動得到——「行高」動的是整段所有內文（含這行，但拖著別的一起動）、
  // 「小標字距」動的是同一行裡字與字之間、「小標字級」動的是那行字多大（行距是跟著字級走
  // 的比例，字放大了兩行之間只會更遠）、「標題塊裡面」調的是小標跟大標之間那截 margin，
  // 同一行字自己換行的距離不歸它管、「段落大標行距」落在 h2 上，這行是 p。
  // 收緊給 1.15 不給更小：中文的字在行框裡本來就佔滿，1.0 以下上下兩行會真的疊到筆畫。
  eyebrowLeading: ["tight", "normal", "loose"],
  // 分隔線（上 / 下 / 上下都有 / 沒有）
  divider: ["none", "top", "bottom", "both"],
  // 該 section 標題字級（small 0.85x / default 1x / large 1.25x）
  headingScale: ["small", "default", "large"],
  // 該 section 最低高度（auto 不限制 / tall 80vh / fullscreen 100vh）
  minHeight: ["auto", "tall", "fullscreen"],
  // 內容垂直位置（top 靠上 / middle 置中 / bottom 靠下）。只有在這一段比內容高的時候才看得出
  // 差別，也就是設了上面那條「最低高度」之後——原本撐出來的空高一律留在內容下面，商家選了
  // 滿屏是想要一整螢幕的段落，拿到的是一小塊內容黏在上緣、下面一大片空白。
  contentAlign: ["top", "middle", "bottom"],
  // 這一段在哪台裝置不顯示（none 都顯示 / mobile 手機不顯示 / desktop 桌機不顯示）。
  // 同一份內容在手機與桌機不會一樣好看：橫排的合作 logo、6 張一列的照片牆在手機上會擠成
  // 一長條，商家只能整段關掉（那條開關是全站的，桌機也跟著沒了）；反過來手機專用的「直接
  // 打電話」那類段落在桌機上是多餘的。原本沒有「只在某台裝置不顯示」這一格。
  // 平板一律顯示：只有一欄，選了「手機不顯示」還要決定平板算不算手機，切在中間最好解釋
  // ——手機是 640 以下、桌機是 1024 以上，中間那段兩邊都不碰。
  hideOn: ["none", "mobile", "desktop"],
  // 照片圓角（soft 14px / round 28px），只套這一段裡的照片，不動段落自己的框。
  // 站上的照片一律是接近直角的（商品卡的圖框固定 4px），這是全站寫死的一個值：
  // 商家把某一段設成圓角卡片（bgColor + borderRadius + shadow 那三件套）之後，段落的四角
  // 圓了、裡面的照片還是方的，兩個圓角對不起來反而更像沒做完；反過來想讓某一段的照片
  // 柔一點（人像、生活情境照），現有的「圓角」那欄動的是整段的外框，照片一點都不會變。
  mediaRadius: ["none", "soft", "round"],
  // 照片比例（square 正方 / portrait 直式 3:4 / landscape 橫式 3:2），只套這一段卡片格線
  // 裡的圖框。每一段的圖框比例是寫死的：選物 3:4、精選商品 1:1、慢讀 5:3、照片牆 1:1，
  // 照片放進去一律照那個框裁——賣水壺、高盆栽這類直式商品的店，商品照在正方形的框裡
  // 被裁頭去尾，商家沒有一格動得到；反過來拍橫幅生活照的店想讓照片牆寬一點也一樣。
  // 圖都是鋪滿框再裁（fill + object-cover），所以換比例是換裁法、不是把圖壓扁。
  mediaAspect: ["auto", "square", "portrait", "landscape"],
  // 照片取景（auto 置中 / top 保留上緣 / bottom 保留下緣），只套這一段卡片圖框裡的照片。
  // 照片鋪滿框再裁的時候一律從正中間取——直式商品照放進正方或橫式的框，被裁掉的是上下
  // 兩頭，而盆栽的葉冠、水壺的瓶口偏偏都在上面，裁掉的剛好是重點；上一欄「照片比例」
  // 換的是框的形狀，救不了「同一個框裡該留哪一端」這件事，商家原本沒有一格能選。
  // 只給上下兩檔不給左右：卡片圖框永遠比照片窄邊裁長邊，直式照片在框裡被裁的是上下，
  // 左右那組要等到有橫式照片配直式框的實際案例再說，先不擺一排按了沒反應的按鈕。
  mediaFocus: ["auto", "top", "bottom"],
  // 照片放不放得下整張（cover 鋪滿框裁掉多的 / contain 整張進框、周圍留白）。
  // 上面兩欄一個換框的形狀、一個選被裁時保留哪一端，但兩欄都還在「一定會裁」這個前提裡
  // ——框的比例是固定的，照片的比例每張不同，鋪滿就一定有一邊被切掉。商家最在意的那幾張
  // （整株盆栽的全貌、水壺連把手的側面、有邊框或留白構圖的商品圖）要的不是裁得準一點，
  // 是一點都不要裁，原本沒有一格做得到。選了整張進框之後也一起關掉滑過時的放大——那個
  // 放大會把照片撐出框外再被框裁掉，等於剛留住的邊又切一次，跟商家按這一格的意思相反。
  mediaFit: ["cover", "contain"],
  // 卡片間距（tight 收緊 / loose 放寬），只套這一段排成格子的卡片與照片之間的距離。
  // 商品卡、照片牆、合作 logo 的間距是每段寫死的一組值：商家把欄數調成 4 之後卡片黏在
  // 一起、或想把照片牆做成緊貼的拼貼、把精選商品攤成鬆一點的畫廊感，全都沒有一格動得到
  // ——動得到間距的只有「區段空白」跟「上下外距」，那兩欄調的是段落外圍，卡片彼此之間
  // 一動也不動。收緊 / 放寬蓋掉該段自己的那組值，不跟原值等比（CSS 蓋不掉又乘不了）。
  gridGap: ["tight", "normal", "loose"],
  // 滑過卡片的動作（default 原樣 / calm 只留輕輕浮起 / none 完全不動），只套這一段的卡片。
  // 滑鼠移到商品卡上，站上一律做四件事：卡片浮起、照片放大一成、照片壓一層暗、標題字距
  // 撐開——那是全站寫死的一組動作，每一段都一樣。密集排的照片牆滑過去整片在動、慢讀區
  // 的文章卡被當商品卡放大、拍好構圖的商品照被放大裁掉邊，商家沒有一格關得掉；原本
  // 動得到「動」的只有「進場動畫」，那是整段進場時做一次的事，跟滑過卡片無關。
  // 觸控裝置本來就沒有滑過這件事，這一欄調的是桌機（與平板外接滑鼠）的手感。
  cardHover: ["default", "calm", "none"],
  // 卡片文字位置（auto 跟著整段 / left 靠左 / center 置中 / right 靠右），只套卡片裡
  // 品名、價錢、副標、摘要那幾行。卡片下面的文字自己沒有帶對齊，一律繼承整段容器的
  // 對齊——商家把「標題對齊」設成置中（站上預設就是置中），每張卡的品名與價錢跟著置中，
  // 而商品列表最常見的排法是「段落大標置中、卡片文字靠左」（文字左緣對齊照片左緣，
  // 一整列掃下來才不會每張卡的字都從不同位置開始），原本沒有一格做得到。
  // 還有一個更難解釋的：「內文對齊」那欄的規則落在段落上，卡片裡的價錢、副標、摘要
  // 剛好都是段落，會被一起拉走，品名（h3）不是段落、留在原地——同一張卡上下兩行各自
  // 對齊，商家只是想調段落內文，卡片就先散了。這一欄設的是整張卡（含品名），設了就
  // 兩行一起走，也就把那個散掉的狀態收回來。
  cardText: ["auto", "left", "center", "right"],
  // 卡片外觀（none 原樣 / panel 淡底色的面板 / outline 一圈細框），只套這一段的卡片。
  // 站上的卡片一律是「照片 + 底下裸著的幾行字」直接浮在段落底色上，卡片自己沒有邊界——
  // 那是雜誌感的排法，商品少、留白多的時候好看，但商家把欄數調到 3、4 欄、或每張卡的
  // 品名長短不一之後，一整列看起來就是一堆散字，客人分不出哪行字屬於哪張照片（這也是
  // 網購站的商品卡幾乎都有底或有框的原因）。原本動得到「卡片有沒有邊界」的一格都沒有：
  // 段落自己的「底色 / 外框 / 圓角 / 陰影」那四欄畫的是整段的外圍，一段一個框，不會分到
  // 每張卡身上；「卡片間距」調的是卡片之間的距離，卡片本身還是裸的。
  // 底與框的顏色都從該段的文字色算（currentColor 的淡色），所以深底淺字的段落自動變成
  // 淺色的面板與框，商家不用再挑一次顏色——跟底紋、側邊色條同一個口徑。
  // 設了之後照片自己那圈陰影收掉：卡片已經有邊界了，照片再浮一次會變成框裡還有框。
  cardSurface: ["none", "panel", "outline"],
  // 卡片內距（tight 收緊 / normal 照原本的 14px / loose 放寬），只在上面那欄設了底或框
  // 之後才有東西可調——卡片沒有邊界的時候內距是看不見的空白，調了畫面上沒有任何差別。
  // 設了底或框之後，卡片裡的東西跟框之間留多少一律是寫死的 14px：那個值配站上預設那種
  // 一列三張、品名兩三個字的商品卡剛好，換成商家自己的東西就常常不對。一列一張、照片在
  // 左的清單模式，14px 的框貼著照片邊緣像沒留白；反過來一列四張的小卡、只有品名跟價錢
  // 兩行的段落，同樣 14px 佔掉的比例大得多，一整列看起來鬆散、照片被框擠小。
  // 商家原本沒有一格動得到——「卡片間距」調的是卡片彼此之間的距離、「區段空白」跟
  // 「上下外距」調的是段落外圍，卡片裡面那圈一動也不動。
  // 圓角跟著一起收放：內距收緊還留 14px 的圓角，框的四角會比裡面的照片圓得多，看起來像
  // 兩個對不上的形狀；三檔的圓角照內距等比走（8 / 14 / 22px）。
  cardPadding: ["tight", "normal", "loose"],
  // 卡片排法（stack 照片在上 / side 照片在左），只套這一段的卡片。
  // 站上每一段的卡片都是同一種排法：照片在上、品名價錢在下面一疊——那是格子牆的排法，
  // 適合客人一眼掃過整片照片。但同一批商品換成「一列一張、照片在左、字在右」（也就是
  // 一般網購站的清單模式）時，同一個螢幕高度裡看得到的品項多得多、品名與描述也有寬度
  // 寫得完整，慢讀區那種一段文字配一張圖的卡片更是本來就該橫著排。商家原本沒有一格
  // 做得到：「卡片外觀」給的是邊界、「卡片文字」給的是字站哪、「欄數」調的是一列幾張，
  // 三個都不會把照片從上面搬到左邊。
  // 設成照片在左之後，手機自動收成一列一張——半個螢幕寬的卡片再切成左圖右字，照片只剩
  // 一小格，那不是商家按這一格想要的東西。
  // 多一檔照片在右（side-reverse）：慢讀那種一段文字配一張圖的段落，整站每張卡都照片在左
  // 的話一列列下來像同一張卡複製貼上；左右交錯是雜誌、品牌故事頁本來就在用的排法。
  // 客人的閱讀順序也跟著換——照片在右時先讀到字，主打「先講故事再看照片」的段落用得上。
  cardLayout: ["stack", "side", "side-reverse"],
  // 照片佔寬（auto 照原本的 38% / narrow 25% / wide 50%），只在卡片排法設成照片在左或在右
  // 時有作用。橫著排的那兩檔，照片跟文字的寬度比是寫死的 38 比 62——那個比例是照站上目前
  // 的內容挑的，換成商家自己的東西就常常不對：慢讀那種一段文字配一張圖的段落，照片只佔
  // 三分之一像縮圖，配的圖是橫幅生活照時尤其小；反過來只列品名跟價錢的商品清單，字沒幾個
  // 卻分到六成寬，右邊空一大片。商家原本沒有一格動得到——「一列幾張」調的是一列排幾張卡、
  // 「卡片間距」調的是卡片之間的距離、「照片比例」調的是照片自己是方是長，三個都不改左右
  // 兩欄怎麼分。
  // 只給三檔不給滑桿：這欄調的是同一列每張卡的共同骨架，能拖到 41%、43% 只會讓商家在肉眼
  // 分不出來的差別上耗時間，而三檔之間的差是一眼看得出來的（縮圖 / 對半 / 大圖）。
  // 手機上沒作用：640 以下卡片本來就收成上下排，沒有左右兩欄可分。
  cardMediaWidth: ["auto", "narrow", "wide"],
  // 手機一列幾張（auto 照這一段原本的 / one 一列一張 / two 一列兩張），只套 640 以下。
  // 每一段在手機上一列幾張是寫死的：選物、精選、照片牆、數字一律兩張，慢讀、客人的話
  // 一律一張。那組值是照「站上目前的預設內容」挑的，換成商家自己的東西就不一定對——
  // 賣小盆栽、配件的店，一列兩張在手機上每張只剩半個螢幕寬，商品照小到看不出差別；
  // 反過來照片牆放的是橫幅生活照、精選只有兩三樣主打商品時，一列一張才看得清楚。
  // 商家原本沒有一格動得到：「一列幾張」那些欄位（選物 / 精選 / 相簿 columns）調的是
  // 桌機（md 以上）那組，手機那組不跟著動；「卡片間距」只縮距離，張數一樣。
  // 只給一張 / 兩張：手機寬度就那樣，三張以上每張不到三分之一個螢幕，擺了也是給商家
  // 一個按下去必然難看的選項。
  // 跟「卡片排法」的手機規則衝突時以這一欄為準——那邊收成一列一張是沒人選過的自動處理，
  // 這邊是商家自己按的，明確的選擇蓋過自動的預設。
  mobileColumns: ["auto", "one", "two"],
  // 卡片標題行數（auto 照這一段原本的 / one 一行 / two 兩行 / full 完整顯示），只套卡片裡
  // 那行品名或文章標題。精選商品那段的品名寫死只顯示一行、超過就切掉接刪節號（逛街頁、
  // 收藏頁同一套），品名長一點的店（「保溫瓶 500ml 木紋款」這種帶規格的）在首頁只看得到
  // 前半段，客人分不出兩個同系列商品差在哪，商家沒有一格解得開；反過來選物、慢讀那兩段
  // 的標題完全不截，商家自己打了長標題就把那張卡撐高，同一列其他卡片下面空一截。
  // 「卡片文字」設的是那幾行站哪、「卡片外觀」給的是邊界，兩個都不管一行字寫不寫得完。
  // 只管標題不管底下的描述：描述本來就是次要資訊，截掉不影響客人認得出商品，而品名被截
  // 是真的看不懂——先把最痛的那一格補上，描述那格等有店家真的被長描述卡到再說。
  cardTitleLines: ["auto", "one", "two", "full"],
  // 卡片描述行數（auto 照這一段原本的 / one 一行 / two 兩行 / three 三行 / full 完整顯示），
  // 只套卡片裡品名底下那段描述。上一格（卡片標題行數）把品名那行補起來之後，剩下描述這行
  // 還是完全不截：選物那段的副標、慢讀那段的摘要，商家自己打多長就佔多高——一段兩行、
  // 一段五行的話，同一列卡片下緣參差不齊，卡片外觀設成面板或框的時候尤其明顯（一個框矮
  // 一個框高）。反過來想讓摘要多露幾行的店也沒得選，卡片高度完全被最長那段綁住。
  // 「卡片標題行數」只管品名那行、「卡片文字」設的是那幾行站哪，兩個都不管描述佔幾行。
  // 比標題多一檔三行：描述本來就是可以讀幾句的地方，一兩行常常斷在句子中間。
  // 只落在真的是描述的那些行——精選商品品名底下那行是價錢，截掉價錢對客人沒有意義。
  cardDescLines: ["auto", "one", "two", "three", "full"],
  // 卡片標題字級（small 縮一成 / default 照原本的 / large 放大兩成半），只套卡片裡那行品名
  // 或文章標題。上面兩格管的是那行字佔幾行、被不被截，這格管的是那行字本身多大——各段是
  // 寫死的：選物與慢讀 18px（桌機 20px）、精選商品 16px。那組值是照站上預設那種一列三張、
  // 品名兩三個字的卡挑的，換成商家自己的東西常常不對：把欄數調成 2、或卡片排法設成照片在
  // 左的清單模式之後，卡片寬了一倍，16px 的品名在那麼大一張卡上小得像圖說，客人一眼掃過去
  // 先看到的是價錢不是商品；反過來一列四張的小卡、品名又長的店，18px 佔掉卡片下半整整兩行，
  // 照片被擠小。慢讀那種文章卡想讓標題像標題（現在跟商品品名同一級）也一樣沒得調。
  // 商家原本沒有一格動得到——「標題大小」換的是段落大標的字級、「全網站字體大小」動的是
  // 內文那一層、「小標字級」動的是段落最上面那行小標，三個都到不了卡片裡的品名。
  // 用 zoom 不用 font-size：三段的 16 / 18 / 20px 是各段自己挑的差別，寫死一個 font-size 會
  // 把三種拉成同一級（跟內文字級、小標字級那兩欄同一個理由）；zoom 連著行高與底下那截
  // margin 一起縮放，字放大了跟價錢的距離也跟著長，不會出現字變大、間距停在原位的擠感。
  cardTitleScale: ["small", "default", "large"],
  // 卡片標題粗細（normal 照原本的 400 常規 / medium 500 中黑 / bold 700 粗）。卡片上那行
  // 品名（與慢讀的文章標題）在三段裡都是寫死的 400，跟底下那行描述、價錢只差在字大小與
  // 顏色淡一點——商家把卡片調成一列四張的小卡、或把描述字級調大之後，那點差別就不夠了，
  // 客人掃過去分不出哪行是商品名。相反地，慢讀那種一整段摘要的寬卡，400 的標題撐不住
  // 底下那段文字的份量。
  // 商家原本沒有一格動得到：「標題粗細」動的是段落大標（h2），「卡片標題字級」只換大小、
  // 「卡片副文字深淺」動的是描述那層的透明度，都不管品名這行的粗細。
  // 只用有載進來的字重（400 / 500 / 700），不給 300 那種細的——沒載的字重瀏覽器會拿常規
  // 去假變細，中文筆畫會糊掉，跟段落大標那格同一個理由。
  cardTitleWeight: ["normal", "medium", "bold"],
  // 卡片標題行距（tight 收緊 / normal 照原本的 / loose 拉開），指的是卡片上那行品名（或文章
  // 標題）自己換行之後，上下兩行之間隔多遠——只有那行字排到兩行以上才看得出差別。
  // 上面兩格已經讓品名可以變大、可以顯示到兩行甚至完整，而那兩格正好是把品名推成多行的
  // 主因：精選商品原本寫死只顯示一行，商家為了讓「保溫瓶 500ml 木紋款」這種帶規格的品名
  // 看得完整而選了兩行或完整之後，才第一次看到自己的品名換行長什麼樣——每段的行距是跟著
  // 字級 class 來的一個值（選物 1.4-1.56、精選 1.5、慢讀寫死 1.4），照一行字的狀況挑的，
  // 中文換到第二行時上下兩行的筆畫幾乎黏在一起；反過來把品名字級調大之後，同一個比例撐出
  // 來的間隙又大得像兩行沒關係的字。
  // 商家原本沒有一格動得到——「行高」那欄的規則只落在段落那類元素（p / li / blockquote /
  // figcaption / dd）上，卡片裡的品名是 h3，整組跳過；「卡片行距」調的是卡片裡不同行之間
  // （照片到品名、品名到價錢）那截 margin，同一行字自己換行的距離不歸它管；「卡片標題字級」
  // 換的是字多大，行距是跟著字級走的比例，字大了間隙也跟著大。
  // 值寫死不等比：行距本來就是個比例（unitless），三段之間 1.4 與 1.56 的差本來就是字級
  // class 附帶的，不是誰刻意設計的層次——商家按這一格要的是「全部收緊」或「全部拉開」，
  // 跟卡片行距那格（同一張卡上幾截距離有主次，要等比保留）不是同一種東西。
  cardTitleLeading: ["tight", "normal", "loose"],
  // 卡片品名字距（tight 收緊 / normal 照原本的 / wide 撐開），只套卡片上那行品名，不動
  // 描述、價錢與那幾行全大寫小字。上一格補的是品名換行之後上下之間隔多遠，這格補的是同一
  // 行裡字與字之間隔多遠。
  // 字距這一格前面補過三個位置：段落最上面那行小標（eyebrowTracking）、段落大標
  // （headingTracking）、卡片上那幾行全大寫小字（cardMicroTracking）。卡片上字級最大、
  // 客人在一排卡片裡真正拿來認商品的那一行反而沒有——選物 text-lg / sm:text-xl、慢讀同一級，
  // 那是整張卡最大的字。
  // 三段各寫各的：選物與精選的品名沒設字距（跟著整段的 --store-track 走），慢讀那張寫死
  // -0.005em。往內收是照英文品名挑的——英文單字之間的空格還在，收一點看起來是緊實；中文
  // 品名每個字本來就佔滿方框，「保溫瓶」「觀葉植物」這種筆畫多的詞再往內收，字級一大就會
  // 跟隔壁黏在一起。反過來只有兩三個字的品名（「山茶」「鐵壺」），撐開一點才有選物店那種
  // 留白感，現在停在 0 或負值。
  // 商家原本沒有一格動得到——「字距」那欄設的是整段（大標、內文、卡片一起變，要把品名撐開
  // 就得連整段描述一起撐開，一段話的字距一寬就散成一個一個字）、「標題字距」只落在段落那
  // 行大標、「卡片小字字距」只管那幾行全大寫小字、「卡片標題字級」與「卡片標題粗細」動的
  // 是字自己（字大了字距是跟著字級走的比例，字與字的相對密度沒變）。
  // 收緊給 -0.04em 不給更小：品名的字級沒有大標那麼大，再收下去中文筆畫會疊到隔壁的字。
  // 撐開給 0.14em 不給小字那種 0.55em：品名是一個詞要一眼認得出來，撐到那個程度會散成幾個
  // 各自站著的字，而且在窄卡上直接被撐到多換一行（那是全大寫小字那種兩三個字才成立的樣式）。
  cardTitleTracking: ["tight", "normal", "wide"],
  // 卡片描述字級（small 縮一成 / default 照原本的 / large 放大兩成），只套卡片裡品名底下
  // 那段描述（選物的副標、慢讀的摘要）。上一格把品名那行的大小補起來之後，描述這行還是
  // 寫死的 14px——那個值是照站上預設那種一句話的副標挑的，換成商家自己的東西就不對：
  // 慢讀那種一整段摘要的卡片，14px 在寬卡上讀起來像圖說，商家想讓那段真的被讀完就得
  // 整段換字體；反過來一列四張的小卡，品名字級調大之後 14px 的描述跟品名擠在一起分不出
  // 主次，想把描述收小讓品名站出來也沒得調。
  // 商家原本沒有一格動得到——「卡片標題字級」動的是品名那行、「內文字級」那欄的規則落在
  // 段落自己的容器上（管的是段落區塊裡的內文，到不了卡片裡）、「全網站字體大小」動的是
  // 整站那一層，三個都不會只動卡片裡的描述。
  // 用 zoom 不用 font-size（跟上一格同一個理由）：選物那行還同時掛著 sproutly-card-meta
  // （字級與顏色另有一層在管），寫死 font-size 等於跟那層搶；zoom 連著上面那截 mt-1 /
  // mt-3 一起縮放，字放大了跟品名的距離也跟著長，不會出現字變大、間距停在原位的擠感，
  // 卡片描述行數那格的截斷也照樣成立（line-clamp 數的是行，不是絕對高度）。
  // 放大只到兩成不跟品名那格一樣到兩成半：描述比品名大就反過來了，留一階差距。
  cardDescScale: ["small", "default", "large"],
  // 卡片描述行距（tight 收緊 / normal 照原本的 / loose 拉開），只套品名底下那段描述——
  // 上一格動的是那段字多大，這格動的是它排到第二行以後，上下兩行之間隔多遠。
  // 品名那行有這格（cardTitleLeading），描述沒有，但描述才是卡片上一定會換行的那段：
  // 品名多半一行就結束，描述是整句甚至整段，一列三四張的窄卡上兩三行起跳。而現在兩段
  // 描述的行距是各寫各的、都寫死——選物的副標只跟著 text-sm 走（約 1.43，那是英文短句
  // 的密度），慢讀的摘要寫死 1.85。同一個控制在兩段裡落差這麼大，商家在選物那邊看到的
  // 是兩行中文黏成一塊，在慢讀那邊反而是鬆到一段話散開，兩邊要的方向剛好相反。
  // 商家原本沒有一格動得到——段落上「行高」那欄的規則只落在 p 那類元素上（卡片裡的描述
  // 在自己的容器裡，整組跳過）、「卡片行距」動的是照片到品名、品名到描述那幾截 margin，
  // 同一段字自己換行的距離不歸它管、「卡片描述字級」換的是字多大，行距是跟著字級走的
  // 比例，字大了間隙也跟著大，密度沒變。
  // 值寫死不等比（跟品名那格同一個理由）：行距本來就是個比例，1.43 與 1.85 的差是兩段
  // 各自的 class 附帶的，不是誰刻意設計的層次——商家按這一格要的是「收緊」或「拉開」，
  // 兩段一起落到同一個密度才是這格的用處。
  cardDescLeading: ["tight", "normal", "loose"],
  // 卡片描述粗細（normal 照原本的 400 常規 / medium 500 中黑 / bold 700 粗），套的是品名底下
  // 那段描述——選物的副標、慢讀的摘要。粗細這一格前面只做過品名（cardTitleWeight）與價錢
  // （cardPriceWeight），描述那段一直是寫死的 400。
  // 缺這格會怎樣：這兩段描述除了 400 常規，還同時被「卡片副文字深淺」那層淡到 0.7 的
  // 透明度（選物那行掛著 sproutly-card-meta），字本來就比品名小——三個減法疊在一起，那段
  // 話在卡片上淡到像圖說。慢讀那種一整篇文章的摘要卡，摘要才是客人真正要讀的東西，卻是
  // 卡片上最輕的一行；反過來商家把品名調粗（上面那格）之後落差更大，描述整段退到背景裡。
  // 另一邊也一樣沒得救：一列四張的小卡上描述只是一句副標，商家想讓它再退一點、讓品名獨
  // 站出來，也只能整段調深淺，沒辦法只動粗細。
  // 商家原本沒有一格動得到——「卡片標題粗細」動的是品名那行、「卡片描述字級」換的是字多
  // 大（字大了跟粗細是兩回事，放大一段細字只是變成大的細字）、「卡片副文字深淺」動的是
  // 透明度（整行連顏色一起變，淡的字調深會跟品名撞在一起）、段落層的「標題粗細」動的是
  // 段落大標 h2，四個都到不了卡片裡的描述。
  // 只用有載進來的字重（400 / 500 / 700），不給 300 那種細的，跟品名、大標那兩格同一個
  // 理由：沒載的字重瀏覽器會拿常規去假變細，中文筆畫會糊掉。
  // 掛的範圍跟卡片描述字級、行距、行數那幾格一樣是選物 / 慢讀兩段——精選那段同一個位置放
  // 的是價錢，價錢自己有一格粗細（cardPriceWeight），本來就不掛 sproutly-card-desc。
  cardDescWeight: ["normal", "medium", "bold"],
  // 卡片小字字級（small 縮一成 / default 照原本的 / large 放大四成），只套卡片裡那幾行
  // 全大寫、字距撐很開的小字——選物卡片底下的「看更多」、慢讀卡片上面的分類、慢讀卡片
  // 底下的標籤。三行都寫死 10px。10px 這個值是照英文那種 VIEW ALL / JOURNAL 挑的，那
  // 種字全大寫、筆畫少，10px 還讀得出來；換成中文就不行——「看更多」「盆栽照顧」在
  // 10px 裡筆畫糊成一團灰，客人看到的是一條裝飾線不是一行字，而那行常常正是卡片上唯一
  // 告訴客人「這裡可以點」的東西。
  // 商家原本沒有一格動得到——「小標字級」動的是段落大標上面那行（規則落在段落自己的
  // eyebrow 上，到不了卡片裡）、「卡片標題字級」動的是品名、「卡片描述字級」動的是品名
  // 底下那段描述、「全網站字體大小」動的是整站內文那一層，四個都繞過卡片裡的小字。
  // 用 zoom 不用 font-size（跟上面兩格同一個理由）：這幾行的字距是 0.3em / 0.4em 這種
  // 相對值，寫死 font-size 會讓字變大、字距跟著放大到散開；zoom 連著字距與上面那截
  // margin 一起縮放，比例維持原樣。
  // 放大放到四成（10px → 14px）比上面兩格都大：起點只有 10px，兩成才 12px，中文在 12px
  // 還是糊的，這格要能真的把那行救成讀得出來的字才有意義。
  cardMicroScale: ["small", "default", "large"],
  // 卡片小字字距（tight 收緊 / normal 照原本的 0.3-0.4em / wide 再撐開），套的是上一格
  // 那幾行同樣的小字——選物卡片底下的「看更多」、慢讀卡片上面的分類與底下的標籤、精選
  // 卡片上的「剩 N」。上一格把那幾行救回讀得出來的大小，字與字之間空多少還是寫死的：
  // 那組 0.3em / 0.4em 跟段落小標那行的 0.4em 是同一個出發點，照英文全大寫短詞挑的
  // （VIEW ALL、JOURNAL 那種，字母之間本來就有空隙，撐開才有雜誌感）。中文放進去是另
  // 一回事：「看更多」三個字每個之間硬塞進 0.3 個字寬，看起來不是一個詞是三個各自站著
  // 的字；上一格選了「大」之後更明顯——zoom 連字距一起放大，字大了、間隙也跟著大，那行
  // 在手機上直接被撐到換行，一行小字變兩行黏在品名底下。
  // 段落最上面那行小標早就有這一格了（eyebrowTracking），那條規則落在段落自己的 eyebrow
  // 上，到不了卡片裡——同一個問題在卡片裡一直沒得解。「卡片小字字級」換的是那行多大、
  // 「字距」那欄設的是整段（大標與內文一起變，這幾行自己帶著 0.3em / 0.4em 動都不動）。
  // 收緊給 0.12em 不給 0，跟小標那格同一個理由：這種小字的樣式標誌就是那個比內文寬的
  // 字距，收到 0 會變成一行普通小字，看起來像漏排版不是刻意的。
  // 收緊與撐開都是寫死一個值，會把 0.3em 與 0.4em 兩種拉成同一級——這格跟字級那幾格不
  // 一樣，字距沒有等比縮放的寫法（CSS 蓋得掉但乘不了），而商家按這一格就是明確要「全部
  // 收緊」或「全部撐開」，兩行差 0.1em 的原始差別本來就不是他在調的東西。
  cardMicroTracking: ["tight", "normal", "wide"],
  // 卡片價錢字級（small 縮一成 / default 照原本的 / large 放大三成），只套精選商品那段卡片
  // 上品名底下那行價錢。上面三格把品名、描述、小字都補起來之後，價錢是這一組最後一行沒得
  // 動的——它寫死 14px，比品名的 16px 還小，跟選物段的副標同一級。那個安排是照「先看商品
  // 再看價錢」的順序挑的，可是不是每家店都這樣：一株盆栽賣多少常常正是客人在首頁掃過去
  // 唯一在找的東西，14px 的灰字在照片底下幾乎讀不到；反過來把品名字級調大之後，價錢還停
  // 在 14px，一張卡上兩行字重量差太多，價錢看起來像附註。
  // 商家原本沒有一格動得到——「卡片標題字級」動的是品名那行、「卡片描述字級」那組刻意跳過
  // 精選段（同一個位置放的是價錢，不掛 sproutly-card-desc）、「卡片小字字級」動的是全大寫
  // 那幾行、「全網站字體大小」動的是整站內文那一層。
  // 用 zoom 不用 font-size（跟上面三格同一個理由）：價錢那行還同時掛著 sproutly-card-meta
  // （字級與顏色另有一層在管），寫死 font-size 等於跟那層搶；zoom 連著上面那截 mt-1 一起
  // 縮放，字放大了跟品名的距離也跟著長。
  // 放大放到三成（14px → 18px）比品名那格的兩成半多一階：這格存在的意義就是讓商家能把價錢
  // 放到比品名重，卡在跟品名同一級就等於沒得選。
  cardPriceScale: ["small", "default", "large"],
  // 卡片價錢粗細（normal 照原本的 400 常規 / medium 500 中黑 / bold 700 粗），只套精選商品
  // 那段卡片上品名底下那行價錢。這行到現在被補過兩次——字級（放大到 18px）與副文字深淺
  // （拿掉那層額外的淡化）——都是為了同一件事：讓客人在首頁掃過去找得到價錢。剩下沒補的
  // 是粗細，而那是三個裡面最有效的一個：字放大會把卡片下半撐開、顏色調深會跟品名撞成同一
  // 層，粗細不佔空間也不換顏色，是讓一行字站出來最省的作法（網購站的價錢幾乎都是粗的）。
  // 反過來，賣得貴、想讓首頁先講故事的店把品名設成粗之後，價錢跟著留在 400 才對——這格
  // 讓那兩種店各自調得到，不用被同一組寫死的值綁在一起。
  // 商家原本沒有一格動得到：「卡片標題粗細」動的是品名那行（h3）、「標題粗細」動的是段落
  // 大標、「卡片價錢字級」只換大小、「卡片副文字深淺」動的是那層透明度。
  // 只用有載進來的字重（400 / 500 / 700），不給 300 那種細的，理由跟卡片標題粗細那格一樣。
  // 這行沒有 inline 的 font-weight（品名那行有，所以那格得繞 CSS variable），一條規則就蓋
  // 得過去，不用再多一個變數。
  cardPriceWeight: ["normal", "medium", "bold"],
  // 卡片行距（tight 收成一半 / normal 照這一段原本的 / loose 拉開將近一倍），指的是同一張
  // 卡片裡上下幾行之間的距離——照片到品名、品名到描述或價錢、描述到底下那行小字。
  // 前面四格把卡片上每一行的大小都補完了，行與行之間空多少還是寫死的：選物的品名離照片
  // 24px、描述貼著品名 4px、「看更多」再隔 12px；精選 20px / 4px；慢讀 24px / 12px / 12px /
  // 20px。那組值是照站上預設那種一列三張、品名兩三個字的卡挑的，卡片一換樣就不對——
  // 把欄數調成 2 或卡片排法設成照片在左之後，卡片寬了一倍，同一組距離看起來像四行字散在
  // 一大片空白裡；反過來一列四張的小卡，或商家把品名與描述都調大之後，四行字黏成一團，
  // 客人分不出哪行是品名哪行是說明。
  // 商家原本沒有一格動得到——「卡片間距」調的是卡片與卡片之間、「卡片內距」調的是卡片
  // 邊界到內容、「標題與內容」調的是段落大標跟底下那排卡片，三個都不進卡片裡面那幾行之間。
  // 等比縮放不寫死一個值：同一張卡上那幾個距離本來就有主次（描述貼著品名 4px 是「這兩行
  // 是一組」，品名離照片 24px 是「照片結束、文字開始」），全部拉成同一個數字等於把那層
  // 主次抹平——跟內文字級、卡片各字級那幾欄不能寫死 font-size 是同一個理由。
  cardRowGap: ["tight", "normal", "loose"],
  // 卡片副文字深淺（muted 再退一階 / default 照原本的 / strong 跟品名一樣深），指的是卡片上
  // 品名底下那行次要文字有多濃——選物卡片的副標、精選商品卡片的價錢。
  // 那行現在被淡了兩次：顏色本身讀 --store-text-muted（已經是文字色的七成），外面又套一層
  // 全站寫死的 .sproutly-card-meta { opacity: 0.7 }，兩層乘起來只剩不到五成。上一格才把價錢
  // 的字級補成可調，可是價錢淡的主因不是小是淡——一株盆栽賣多少常常是客人在首頁掃過去唯一
  // 在找的東西，字放大了還是一行淺灰，在淺色底或照片旁邊幾乎讀不到。反過來，賣得貴、想讓
  // 首頁先講故事不要先講價的店，會想要它比現在更退一步。
  // 商家原本沒有一格動得到這一層：「內文濃淡」改的是 --store-text-muted，卡片外面那層 0.7
  // 照樣乘上去（選「濃」也只到七成，永遠追不上品名）；「淡化」透明的是整段連照片一起；
  // 「文字顏色」換的是整段的色。
  // 動的是 opacity 那一層不是顏色：那行的顏色是 inline style（規則蓋不過 inline），而且顏色
  // 交給「內文濃淡」管、這格只管卡片上那層額外的淡化，兩格各自獨立、疊起來也講得通。
  // 滑過卡片時那行本來會從 0.7 亮到 1（提示這張卡是活的），三檔都保留那個相對幅度，不會選了
  // 「更淡」之後一滑過去就跳成全黑。
  cardMetaTone: ["muted", "default", "strong"],
  // 外框（subtle 1px / strong 2px，用 outline 避免跟 divider 的 borderTop/Bottom 打架）
  outline: ["none", "subtle", "strong"],
  // 陰影（soft 淺 / deep 深），讓有 bgColor 的 section 像卡片浮起
  shadow: ["none", "soft", "deep"],
  // 圓角（soft 16px / strong 32px），跟 bgColor + outline + shadow 三件套組成卡片風
  borderRadius: ["none", "soft", "strong"],
  // 進場動畫（fade 淡入 / slide-up 上滑），靠 CSS scroll-driven 觸發，edit mode 內 disable
  entrance: ["none", "fade", "slide-up"],
  // 該 section 字體（default 跟全網站 / serif 思源宋體 / sans 思源黑體），讓某段獨立切字體
  fontFamily: ["default", "serif", "sans"],
  // 字距（tight -0.02em / normal 預設 / wide 0.1em），雜誌大標常見 wide
  letterSpacing: ["tight", "normal", "wide"],
  // 行高（tight 1.4 緊湊 / normal 預設不套 / relaxed 2.0 舒展）
  lineHeight: ["tight", "normal", "relaxed"],
  // 淡化（default 不套 / muted 0.85 / faint 0.7），讓次要 section 變淡襯托主角
  opacity: ["default", "muted", "faint"],
  // 濾鏡（grayscale 黑白 / sepia 復古褐），只套這段裡的照片，文字與配色不動
  filter: ["none", "grayscale", "sepia"],
  // 寬度（full 滿版預設 / boxed 置中 1100px / narrow 窄欄 760px）
  sectionWidth: ["full", "boxed", "narrow"],
  // 上下外距（none 貼緊相鄰 / normal 64px / large 112px），配 sectionWidth 讓卡片浮出來
  sectionGap: ["none", "normal", "large"],
  // 標題粗細（light 400 常規 / normal 不套維持原樣 / bold 700 粗）。只用思源黑體 / 宋體有
  // 載進來的字重（400 / 700），不挑 300 之類沒載的——瀏覽器會拿常規去假變細，中文筆畫糊掉。
  headingWeight: ["light", "normal", "bold"],
  // 段落大標行距（tight 收緊 / normal 照原本的 / loose 拉開），指的是段落那行大標自己換行
  // 之後，上下兩行之間隔多遠——只有標題排到兩行以上才看得出差別。
  // 卡片上那行品名早就有這一格（cardTitleLeading），底下的描述也有（cardDescLeading），
  // 段落自己的大標反而沒有，而大標才是最容易換行的那一行：字級本來就是全段最大的
  // （慢讀那段桌機 2.5rem），商家打的又常常是一整句中文（「來自南投山區的手作陶盆」），
  // 在手機上一行放不下是常態。而各段大標的行距是跟著 Tailwind 字級 class 附帶來的一個值
  // （text-2xl 那組約 1.33、text-3xl / text-4xl 那組壓到 1.1-1.2），那組值是照英文標題挑的
  // ——英文字母沒有上下伸得那麼滿的筆畫，1.1 倍看起來剛好；中文換到第二行時上下兩行的
  // 筆畫幾乎貼在一起，整塊標題糊成一團。反過來把標題大小調成小之後，同一個比例撐出來的
  // 間隙又鬆得像兩行沒關係的字。
  // 商家原本沒有一格動得到——「行高」那欄的規則刻意只落在段落那類元素（p / li /
  // blockquote / figcaption / dd）上、明講不碰 h1-h3；「標題大小」與「標題粗細」動的是字
  // 自己（字大了行距是跟著字級走的比例，密度沒變）；「標題塊裡面」調的是小標跟大標之間、
  // 大標跟引言之間那截 margin，同一行字自己換行的距離不歸它管。
  // 值寫死不等比（跟卡片標題行距那格同一個理由）：行距本來就是個比例，各段 1.1 與 1.33
  // 的差是字級 class 附帶的、不是誰刻意設計的層次，商家按這一格要的是「全部收緊」或
  // 「全部拉開」，兩段一起落到同一個密度才是這格的用處。
  // 收緊給 1.1 不給更小：中文的字在行框裡本來就佔滿，1.0 以下上下兩行會真的疊到筆畫。
  // 不套 hero — hero 主標的字級 / 顏色 / 對齊自成一組控制，跟標題粗細、底線那幾格同一條線。
  headingLeading: ["tight", "normal", "loose"],
  // 段落大標字距（tight 收緊 / normal 照原本的 / wide 撐開），只套段落那行大標，不動小標
  // 與內文。上一格補的是大標換行之後上下之間隔多遠，這格補的是同一行裡字與字之間隔多遠。
  // 段落最上面那行小標早就有這一格（eyebrowTracking），卡片上那行小字也有
  // （cardMicroTracking），字級最大、最先被客人看到的大標反而沒有。
  // 各段大標的字距一律寫死 -0.01em（數字那段是 -0.02em），是照英文標題挑的：英文標題收一點
  // 字距，單字之間的空格還在、看起來是緊實不是擠。中文沒有那個空格，每個字本來就佔滿自己的
  // 方框，再往內收 0.01 個字寬，筆畫多的字（「選」「盤」「灣」）會跟隔壁的黏在一起，字級
  // 越大黏得越明顯——慢讀那段桌機 2.5rem，那是整頁最大的一行字，也是最需要透氣的一行。
  // 反過來大標只有兩三個字（「關於」「聯絡」）時，撐開一點才有雜誌那種留白感，現在也停在
  // 同一個負值。
  // 商家原本沒有一格動得到——「字距」那欄設的是整段（大標、內文、數字一起變，要把大標撐開
  // 就得連內文一起撐開，一段話的字距一寬就散成一個一個字）、「標題大小」與「標題粗細」動的
  // 是字自己（字大了字距是跟著字級走的比例，字與字的相對密度沒變）、「小標字距」只管最上面
  // 那行小標。
  // 收緊給 -0.05em 不給更小：再收下去中文的筆畫會真的疊到隔壁的字，那不是緊實是印壞。
  // 撐開給 0.08em 不給小標那種 0.4em：大標是要一整句讀得順，撐到小標那個程度，一句話會散
  // 成一個一個沒關係的字（那是小標那種兩三個詞才成立的樣式）。
  // 不套 hero — hero 主標的字級 / 顏色 / 對齊自成一組控制，跟標題粗細、行距那幾格同一條線。
  headingTracking: ["tight", "normal", "wide"],
  // 底紋（grid 細格線 / dots 點陣 / lines 斜紋），純 CSS gradient 疊在底色上，不吃圖檔。
  // 線的顏色走 currentColor，所以深底淺字的 section 換成淺色紋、不用另外設一組顏色。
  texture: ["none", "grid", "dots", "lines"],
  // 底色明暗變化（top 上緣加重 / bottom 下緣加重 / vignette 四周暈影），跟底紋同樣走
  // currentColor：淺底深字的段落疊出來是變暗，深底淺字的段落疊出來是提亮，不用另挑顏色。
  bgGradient: ["none", "top", "bottom", "vignette"],
  // 標題底線（short 短線 / full 整條），畫在該段 h2 底下。顏色跟外框、分隔線同一個口徑
  // （自訂文字色算出來的淡色），所以深底淺字的段落自動變成淺色線、不用另挑一次顏色。
  headingRule: ["none", "short", "full"],
  // 標題底線的粗細（thin 1px / normal 照原本的 2px / thick 4px），只在上面那欄設了線之後
  // 才有東西可調。線的長度有短線與整條兩檔可選，粗細一直是全站寫死的一個 2px——那個值配
  // 站上預設那種中等字級的大標剛好，但商家把某一段的標題大小調成大之後，2px 的線在放大的
  // 字底下細得像沒畫；反過來標題設成小、或整段只是照片牆的一行小標題時，同樣一條 2px 橫過
  // 整個螢幕寬（整條那檔）比標題本身還搶眼。
  // 商家原本沒有一格動得到——「標題底線」只換長度、「標題大小」與「標題粗細」動的是字自己，
  // 線不會跟著長。
  // 只給三檔不給滑桿：線是配著標題看的東西，1 / 2 / 4px 之間的差一眼看得出來，中間那些
  // 半格的差別在螢幕上根本畫不出來（非整數的線會被瀏覽器抹成灰邊）。
  headingRuleWeight: ["thin", "normal", "thick"],
  // 側邊色條（left 左緣 / right 右緣），畫在該段的左或右邊緣，4px 粗。分隔線佔的是
  // borderTop/Bottom、外框走 outline，三者不互相蓋。顏色比照外框與分隔線：該段設了
  // 文字色就從它算（深底淺字自動變淺色條），沒設就用全站主色 accent。
  accentBar: ["none", "left", "right"],
} as const satisfies Record<string, readonly string[]>;

// 每一欄「等同沒設定」的那個值。editor 端商家選到它就把整欄 delete 掉（少一欄存進 DB，
// 也讓「有沒有自訂」這件事只看 key 在不在）。沒列在這裡的欄位（headingAlign / paddingScale
// / headingScale / minHeight）沒有這種值，只有明確按重設才清掉。
export const SECTION_STYLE_NEUTRAL_VALUES = {
  bodyAlign: "auto",
  bodyMeasure: "auto",
  bodyScale: "default",
  bodyTone: "default",
  bodyWeight: "normal",
  headingTone: "default",
  contentAlign: "top",
  headingGap: "normal",
  headingInnerGap: "normal",
  eyebrowTracking: "normal",
  eyebrowScale: "default",
  eyebrowWeight: "normal",
  eyebrowLeading: "normal",
  hideOn: "none",
  divider: "none",
  mediaRadius: "none",
  mediaAspect: "auto",
  mediaFocus: "auto",
  mediaFit: "cover",
  gridGap: "normal",
  cardHover: "default",
  cardText: "auto",
  cardSurface: "none",
  cardPadding: "normal",
  cardLayout: "stack",
  cardMediaWidth: "auto",
  mobileColumns: "auto",
  cardTitleLines: "auto",
  cardDescLines: "auto",
  cardTitleScale: "default",
  cardTitleWeight: "normal",
  cardTitleLeading: "normal",
  cardTitleTracking: "normal",
  cardDescScale: "default",
  cardDescLeading: "normal",
  cardDescWeight: "normal",
  cardMicroScale: "default",
  cardMicroTracking: "normal",
  cardPriceScale: "default",
  cardPriceWeight: "normal",
  cardRowGap: "normal",
  cardMetaTone: "default",
  outline: "none",
  shadow: "none",
  borderRadius: "none",
  entrance: "none",
  fontFamily: "default",
  letterSpacing: "normal",
  lineHeight: "normal",
  opacity: "default",
  filter: "none",
  sectionWidth: "full",
  sectionGap: "none",
  headingWeight: "normal",
  headingLeading: "normal",
  headingTracking: "normal",
  texture: "none",
  bgGradient: "none",
  headingRule: "none",
  headingRuleWeight: "normal",
  accentBar: "none",
} as const satisfies Partial<{
  [K in keyof typeof SECTION_STYLE_ENUMS]: (typeof SECTION_STYLE_ENUMS)[K][number];
}>;

// `-readonly`：欄位表是 as const（整份唯讀），若不脫掉，推出來的型別每一欄都變唯讀，
// 編輯器那邊 `next.sectionGap = ...` / `delete next.opacity` 這種改法會整排編譯不過。
type SectionStyleEnums = {
  -readonly [K in keyof typeof SECTION_STYLE_ENUMS]?: (typeof SECTION_STYLE_ENUMS)[K][number];
};

// 顏色欄位跟上面的選項欄位規則不同（吃任意 hex，且 null 是「明確清掉、回到 theme 預設」
// 這個有意義的狀態，跟「沒設定」不一樣），所以獨立列。
export interface SectionStyle extends SectionStyleEnums {
  bgColor?: string | null; // null = 用 theme.bg；hex = 覆寫
  textColor?: string | null; // null = 用 theme.text；hex = 覆寫（深底配淺字常用）
}

// 編輯器改某一段樣式時送的 patch：沒提到的欄位不動，給合法值就設，給 null 就清掉這一欄。
export type SectionStylePatch = Partial<{
  [K in keyof typeof SECTION_STYLE_ENUMS]: (typeof SECTION_STYLE_ENUMS)[K][number] | null;
}> & {
  bgColor?: string | null;
  textColor?: string | null;
};

// 把 patch 疊到現有樣式上，回一份新的（不改原物件——編輯器的 undo history 靠每步一份新值）。
// 選到「等同沒設定」的那個值（見 SECTION_STYLE_NEUTRAL_VALUES）跟給 null 一樣清掉整欄。
// 顏色兩欄照舊：null 是「清掉覆寫回 theme 預設」這個有意義的狀態，要留著存回去。
export function applySectionStylePatch(
  current: SectionStyle,
  patch: SectionStylePatch
): SectionStyle {
  const next: SectionStyle = { ...current };
  const neutral = SECTION_STYLE_NEUTRAL_VALUES as Partial<Record<string, string>>;

  for (const field of Object.keys(SECTION_STYLE_ENUMS) as (keyof typeof SECTION_STYLE_ENUMS)[]) {
    const v = patch[field];
    if (v === undefined) continue;
    if (v === null || v === neutral[field]) delete next[field];
    else (next as Record<string, unknown>)[field] = v;
  }

  if (patch.bgColor !== undefined) next.bgColor = patch.bgColor;
  if (patch.textColor !== undefined) next.textColor = patch.textColor;
  return next;
}

// section key 的長度上限。存檔那層本來就擋（避免有人塞一串垃圾當 key 把 theme jsonb 撐爆），
// 讀那層以前沒擋——同一條線只守一半，改成兩層共用同一個值。
const MAX_SECTION_KEY_LENGTH = 60;

// 清洗單一個 section 的樣式覆寫。認不得的欄位、不在合法值內的值一律丟掉（不是報錯——
// 舊資料、手改過的 jsonb 都可能有殘留，丟掉那一欄比整筆不讓商家存好）。
// 清完一欄都不剩就回 null，呼叫端不留這個 key。
export function sanitizeSectionStyle(raw: unknown): SectionStyle | null {
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) return null;
  const obj = raw as Record<string, unknown>;
  const entry: Record<string, unknown> = {};

  for (const [field, allowed] of Object.entries(SECTION_STYLE_ENUMS)) {
    const v = obj[field];
    if (typeof v === "string" && (allowed as readonly string[]).includes(v)) {
      entry[field] = v;
    }
  }

  // 顏色：清得出 hex 就用清過的；明確給 null 代表「清掉覆寫」，也要留著（跟「沒設定」
  // 在畫面上同結果，但商家按了重設就該存回去，不能被當沒設定而保留舊值）。
  for (const field of ["bgColor", "textColor"] as const) {
    const hex = normalizeHexColor(obj[field]);
    if (hex) entry[field] = hex;
    else if (obj[field] === null) entry[field] = null;
  }

  // 只要有任何一欄過關就留下這個 section 的覆寫。以前是一長串 entry.X !== undefined 的
  // 手寫 or，每加一個控制就要記得補一項，漏掉那個控制單獨設定時整筆會被丟掉。
  if (Object.keys(entry).length === 0) return null;
  return entry as SectionStyle;
}

// 清洗整份 sectionStyles（key = section id）。公開頁讀回與編輯器存檔走同一支，
// 兩邊對「什麼算合法」的認定不可能再各自漂移。
export function sanitizeSectionStyles(raw: unknown): Record<string, SectionStyle> {
  const result: Record<string, SectionStyle> = {};
  if (!raw || typeof raw !== "object" || Array.isArray(raw)) return result;
  for (const [k, v] of Object.entries(raw as Record<string, unknown>)) {
    if (!k || k.length > MAX_SECTION_KEY_LENGTH) continue;
    const entry = sanitizeSectionStyle(v);
    if (entry) result[k] = entry;
  }
  return result;
}
